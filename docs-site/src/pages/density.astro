---
import Base from '../layouts/Base.astro';
import { DensityShowcase } from '../components/DensityShowcase.tsx';
---
<Base title="Density">
  <a href="/">&larr; Home</a>
  <h1>Density</h1>
  <p>The same real <code>Button</code> + <code>Table</code> row, each wrapped in a real <code>Density</code> region — see <a href="/components/density/">the Density doc</a> for the underlying component, or <a href="/tokens/">Tokens</a> for the color/spacing/type scale it's built from.</p>
  <DensityShowcase client:load />
</Base>
