# Busy Office UI Intent

Provide a portable, reusable visual component package and reference page
compositions for Busy Office applications. The package is a UI dependency, not
an ERP platform kernel or application implementation.

Consumers own business behavior, data, navigation registration, permissions,
and runtime integration. This repository owns components, styling compilation,
fonts, documentation, and pure examples.
